// define task in sub project 1 (2 also has the same name task)
task name {
    println 'sub project 1'
}

// define task only in sub project 1
task sub1 {
    println 'sub1'
}

task showVersion {
    println 'in sub1 version : ' + myVersion()
}

task show << {
    // Use inherited property
    println 'srcDirName: ' + srcDirName

    // Use inherited method
    File srcDir = getSrcDir(project)
    println 'srcDir: ' + rootProject.relativePath(srcDir)
}